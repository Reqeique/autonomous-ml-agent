# Secrets.env.env.*!.env.examplekaggle.json~/.kaggle/wandb/*.wandb# Python__pycache__/*.pyc*.pyo.ipynb_checkpoints/.venv/venv/# Editor / OS.DS_StoreThumbs.db.idea/.vscode/# Local artifacts pulled by the agentartifacts/*.log# Firecrawl research dumps (local-only).firecrawl/# Real pending state is fine to commit (small JSON), but allow ignoring if you want ephemeral:# state/pending_training.json